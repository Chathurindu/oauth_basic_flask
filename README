# create virtual envirenment
# activate virtual envirenment
# install requirements using :> pip -r install requirements
# create OAuth 2.0 Client IDs credentials and add both http://localhost:5000 and http://127.0.0.1:5000 to URIs then,
    add http://localhost:5000/login, http://localhost:5000/authorize, http://127.0.0.1:5000/authorize, 
    http://localhost:5000/request-api, http://127.0.0.1:5000/request-api, http://localhost:5000/sheetauth,
    http://127.0.0.1:5000/sheetauth to Authorized redirect URIs
# create a service account and get a email and add that email to googlesheet access email.
# create .env file and add client_id, client_secret and SAMPLE_SPREADSHEET_ID(google sheet id)
# create a client.json file and add add service account credentials
# run the program using :> flask run
# http://127.0.0.1:5000/